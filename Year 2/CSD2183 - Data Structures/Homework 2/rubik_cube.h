#ifndef RUBIK_CUBE_H
#define RUBIK_CUBE_H // Define macro to ensure that the header file's contents are only included once, avoiding potential redefinition errors.

#include <iostream>
#include <queue>
#include <unordered_map>
#include <vector>
#include <string>

using namespace std;

string solveCube(const string& scrambledState); 

#endif // RUBIK_CUBE_H
